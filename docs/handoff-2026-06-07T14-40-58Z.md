# Handoff — 2026-06-07T14-40-58Z (Continuous audit system + bio/pendidikan corruption detection)

## Goal
- Implement continuous file-based audit system (`audit_corruption.py` + `audit_cron.sh`) untuk mendeteksi bio corruption (Lesson #11) + pendidikan field pollution, deploy via scheduled cron 02:00 WIB, dan lay foundation untuk multi-agent parallel + GitHub issue tracking untuk ambiguous cases.

## Constraints & Preferences
- Conservative approach: file-based (0 API calls) sebagai primary, on-demand API untuk ambiguous
- Slow but continuous update (daily batch kecil, bukan parallel massive)
- Cron "audit" = `audit_cron.sh` (02:00 WIB daily, 10 files/run, single agent)
- Pakai `/usr/bin/python3` (Lesson #13: homebrew Python 3.14 expat broken)
- TDD discipline (RED → GREEN) — 32/32 unit tests pass
- TIDAK commit `__pycache__/`, `.omo/`, atau `osint_archive/audit_state_agent_*.json`
- Multi-agent parallelism aman (Lesson #12 race condition prevention)
- Per agent: disjoint file set + per-agent state file + unique quarantine filenames
- Existing convention: Lesson #10 (audit trail via `osint_archive/quarantine/`)
- Lesson #11 corruption patterns: garbage first sentence, Wikipedia+name mismatch, bio about wrong person
- User pattern: "pelan-pelan tapi bisa terus update"
- Field name convention: detail files use `education` (English) and `kary` (not `pendidikan`/`karya`)

## Progress

### Done
- 4 commits in this session lineage: e6f0e43, 4caff37, 7484aea, plus new 205abc9 (audit system)
- `audit_corruption.py` (633 lines): Phase 1 static audit, multi-agent safe partitioning, per-agent state, quarantine
- `audit_cron.sh` (126 lines): flock mutual exclusion, random delay 0-15m, single/multi-agent modes
- `test_audit_corruption.py` (356 lines): 32 TDD tests covering bio/pendidikan/partition/state/quarantine/CLI/GH integration
- `.gitignore`: exclude per-run `audit_state_agent_*.json`
- Local full scan: 144 audited → 6 clean + 44 corrupt + 13 ambiguous + 81 incomplete
- Server pull: `Updating 7484aea..205abc9`
- Server tests: 32/32 pass
- Server audit smoke test: 5 files audited, mixed results (clean/incomplete/corrupt as expected)
- Cron entry deployed: `0 2 * * * TZ=Asia/Jakarta /home/ubuntu/profilasatidz/audit_cron.sh`
- `flock` verified available: `/usr/bin/flock`
- AGENTS.md Lesson #16 added: continuous audit via scheduled cron

### In Progress
- Final commit + push (AGENTS.md + handoff)

### Blocked
- (none)

## Key Decisions
- **Opsi A (cron terpisah 02:00 WIB) > Opsi B/C**: independent, reliable, no coupling dengan enrichment
- **Single agent (MAX_AGENTS=1) by default**: conservative, no parallel risk, easy to reason about
- **Audit is non-destructive by default**: only writes state, no auto-quarantine. User must explicitly run `--quarantine` to clean
- **Field name aliasing**: `_get_field(detail, "pendidikan", "education")` — handles both conventions, forward-compat
- **Phone regex pitfall fixed**: year ranges like "1996-1998" no longer false-positive flagged
- **BIO_PREFIX_MARKERS added**: catches "Ceramah", "Audio", "Download", "Beliau", "Ia adalah" etc. in education list
- **INSTITUTION_MARKERS whitelist**: legitimate school names (Universitas, Ma'had, LIPIA, etc.) pass
- **No-institution-marker fallback**: entries > 80 chars without institution marker flagged
- **f-lock prevents concurrent runs**: Lesson #12 prevention
- **State resume verified**: 144 → 139 unprocessed after 5 audited (deterministic, atomic write)
- **State file naming**: `audit_state_agent_{id}.json` for parallel isolation

## Next Steps
- Commit AGENTS.md + this handoff
- Push to origin
- (Optional) Monitor first cron run at 02:00 WIB next day — check `osint_archive/audit_cron-{date}.log`
- (Future) Add GH issue creation for ambiguous cases (create_audit_issues.py) — currently only state tracking
- (Future) Phase 2 (targeted API for ambiguous) — current implementation has the field but not the verification flow
- (Future) Auto-quarantine flow with confirmation prompt
- (Future) Multi-agent parallel mode (currently single-agent by default)

## Critical Context
- **Server**: `juragan` (43.156.90.61, ubuntu user, /home/ubuntu/profilasatidz)
- **SSH key**: `~/.ssh/juragandata` (id_rsa)
- **Python**: `/usr/bin/python3` = Python 3.9.6 (no `str | None` union syntax — use `from __future__ import annotations`)
- **Cron entries on server** (4 total):
  - `0 * * * *` nyicil_cron.sh (every hour)
  - `0 16 * * *` nyicil_review.py (daily 16:00, profiling+enrichment)
  - `0 */6 * * *` guardian_cron.sh (every 6h)
  - `0 2 * * * TZ=Asia/Jakarta audit_cron.sh` (daily 02:00 WIB, NEW)
- **State file path**: `osint_archive/audit_state_agent_0.json` (single agent default)
- **Log file path**: `osint_archive/audit_cron-{YYYY-MM-DD}.log`
- **Lock file**: `osint_archive/audit.lock` (flock)
- **Quarantine dir**: `osint_archive/quarantine/` (Lesson #10 convention)
- **144 detail files locally**, server has production data (different state)
- **Base corruption scope**: 44 corrupt + 13 ambiguous of 144 = 39.6% pollution rate (local baseline)
- **Audit verdicts**: clean | corrupt | ambiguous | incomplete
- **Aggregated verdict** (audit_one): if any field is corrupt → whole file corrupt; ambiguous → ambiguous; incomplete → incomplete; else clean

## Relevant Files
- `audit_corruption.py`: NEW (Phase 1 static audit + multi-agent partitioning + state + quarantine)
- `audit_cron.sh`: NEW (cron wrapper with flock + log + multi-agent option)
- `test_audit_corruption.py`: NEW (32 TDD tests, all pass on local + server)
- `.gitignore`: MODIFIED (exclude `osint_archive/audit_state_agent_*.json`)
- `AGENTS.md`: MODIFIED (Lesson #16: continuous audit via scheduled cron)
- `wikifetch.py`: from previous session (Lesson #14 prevention, not used by audit_corruption.py yet)
- `nyicil_enrich.py`, `nyicil_review.py`, `enrich_sanad.py`: from previous session (Lesson #14 integrated)
- `osint_archive/audit_state_agent_0.json`: runtime state (gitignored)
- `osint_archive/audit_cron-{date}.log`: runtime log (gitignored via *.log)
- `docs/handoff-2026-06-07T14-40-58Z.md`: THIS FILE
- `docs/handoff-2026-06-07T20-32-00Z.md`: previous handoff (Lesson #14 prevention)
- `docs/handoff-2026-06-07T20-15-00Z.md`: previous handoff (corruption cleanup)
