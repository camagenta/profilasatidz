# Handoff — 2026-06-06T10-50-00Z (JSON CRUD Admin Panel + Path/Credential Update)

## Goal
- Implementasi CRUD Admin Panel berbasis JSON + konfigurasi path dan credential kustom.

## Constraints & Preferences
- HTTP Basic Auth via env vars `ADMIN_USER` / `ADMIN_PASS`
- Path prefix `/dapur/` (hardcoded di route handler)
- Atomic write fallback untuk Docker bind mount
- Semua operasi admin mengirim notifikasi Telegram via Hermes
- Git commit style: `feat(admin): ...`

## Progress
### Done
- 7 route admin: Dashboard, List, Edit Master, Edit Detail, Delete
- Helper: `adminAuth()`, `atomicWriteJSON()`, `calcCompleteness()`, `completenessBadge()`
- Atomic write fallback (tmp+rename gagal di bind mount → direct write)
- Path: `/admin/` → `/dapur/`
- Credential: `admin`/`admin123` → `juragan`/`Bismillah.3x!` (via env var)
- Hermes notification tiap operasi admin
- Docker build & restart, semua endpoint terverifikasi

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- **Path hardcoded**: Route path `/dapur/` masih hardcoded di `main.go`. Untuk ganti tanpa rebuild perlu refactor ke konfigurasi.
- **Credential via env var**: `ADMIN_USER` / `ADMIN_PASS` dari `.env` + `docker-compose.yml`, bukan hardcoded.
- **Atomic write fallback**: `os.Rename` gagal di Docker bind mount → fallback `os.WriteFile` langsung.

## Next Steps
- Pembersihan 15 profil completeness 0% (bisa via admin panel)
- Migrasi SQLite (Issue #2) jika skala membesar
- Redirect `/admin/` → `/dapur/` agar tidak bingung

## Critical Context
- Admin panel: `http://localhost:8080/dapur/`
- Credential: `juragan` / `Bismillah.3x!` (bisa diubah di `.env`)
- Hermes path: `/home/ubuntu/.hermes/hermes-agent/venv/bin/python -m hermes_cli.main send --to telegram`

## Relevant Files
- `/home/ubuntu/profilasatidz/main.go`: +700 baris admin routes
- `/home/ubuntu/profilasatidz/docker-compose.yml`: env vars
- `/home/ubuntu/profilasatidz/.env`: credentials lokal (gitignored)
