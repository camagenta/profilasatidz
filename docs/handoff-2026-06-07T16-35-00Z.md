# Handoff — 2026-06-07T16-35-00Z (Profile Issue Guardian + Auto-Create Fallback)

## Goal
- Memastikan SETIAP profil asatidz punya GitHub Issue agar fitur kontribusi selalu available
- Membangun fallback system: jika user kontribusi ke profil yang belum ada issue, sistem auto-create
- Triggered by: laporan kontributor yang tidak menemukan issue untuk "Ustadz Abdul Hakim" (salah satu dari 167 profil yang belum punya issue)

## Constraints & Preferences
- Cron max 4x/hari (setiap 6 jam) untuk avoid GitHub rate limit
- Random delay 0-15 menit sebelum eksekusi (spread load)
- Polite GitHub API: 3 detik delay antar issue creation
- Tidak replace issue yang sudah ada (skip by name match)
- Body issue mengikuti format dari `reformat_and_comment_issues.py` (Profil Live + Status Profiling + Sumber)
- Git commit style: `feat(guardian): ...`
- Path hardcoded di Go tetap dipakai (sesuai lesson #6 di AGENTS.md)

## Progress
### Done
- `profile_issue_guardian.py` (499 baris): scanner + auto-creator + Telegram notifier
  - Dual search: by label + by title prefix (catches mislabeled)
  - Whitespace normalization untuk handle "Abu Salma Muhammad" vs "Abu Salma muhammad"
  - Container-first read dengan local fallback
  - KajianLive.my.id info awal di-embed ke body issue
  - Log ke `osint_archive/guardian-{date}.log`
- `guardian_cron.sh`: cron wrapper dengan rate limit harian
- `main.go` modificasi:
  - `createGitHubIssue()`: helper baru (165 baris) untuk create issue via GitHub API
  - `boolStatus()`, `computeCompletenessFromDetail()`: helper untuk body issue
  - `handleContribute()`: fallback logic - jika issue tidak ditemukan, auto-create
  - Hermes notification ke Telegram untuk setiap fallback create
- `go build main.go` clean ✓
- Commit `f23e007` pushed to origin/main

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- **Dual search strategy**: search by label + by title prefix, karena beberapa issue tidak punya label tapi punya judul yang benar. Hasil: 115 issue terdeteksi (vs 126 issue total di repo - 11 sisanya adalah issue non-profil seperti [Data], [FE], [OSINT])
- **Skip existing issues** by case-insensitive name match, bukan ID. Lebih robust karena ada beberapa varian slug.
- **Telegram notifier**: panggil Hermes hanya jika `--notify` flag aktif, supaya cron silent run tidak spam Telegram.
- **167 profiles missing**: distribusi:
  - 168 profil completeness 0% (mayoritas missing)
  - 141 dengan bio
  - 89 dengan foto
  - 142 dengan detail
- **Fallback path di Go**: prioritas load dari `detail/{id}.json` agar issue baru punya data lengkap. Jika tidak ada, tetap buat issue dengan placeholder "Belum ada deskripsi biografi".

## Next Steps
- **IMMEDIATE**: jalankan `python3 profile_issue_guardian.py --notify` (live, BUKAN dry-run) untuk create 167 issue yang missing. Ini akan butuh ~10-15 menit (3s x 167 + overhead).
- Setelah issue dibuat, review distribusi (kemungkinan 100+ akan auto-detect sebagai data baru).
- Update `enrich_kajianlive.py` atau script terkait untuk handle issue baru ini di run berikutnya.
- Tambahkan redirect `/admin/` → `/dapur/` di `main.go` (carry-over dari handoff sebelumnya).
- Pertimbangkan SQLite migration jika 167 issue + kontribusi masuk membuat JSON tidak scalable (sesuai Issue #2 di repo).

## Critical Context
- **Repo**: `camagenta/profilasatidz` (label: `profil-asatidz`)
- **Container**: `profilasatidz` (port 8080), bind mount: `./asatidz_master.json` → `/root/asatidz_master.json`
- **Hermes path**: `/home/ubuntu/.hermes/hermes-agent/venv/bin/python -m hermes_cli.main send --to telegram`
- **Issue title pattern**: `[Profil Asatidz] {profile_name}` (case-insensitive match)
- **Body pattern**: `# {name}` + `## 📝 Profil Live` + `## 📊 Status Profiling` + `## 🤝 Cara Berkontribusi`
- **Cron schedule** (recommended):
  ```bash
  0 */6 * * * /home/ubuntu/guardian_cron.sh
  ```
- **Token**: `GITHUB_TOKEN` di `.env` (di-share ke container via `docker-compose.yml`)

## Relevant Files
- `profile_issue_guardian.py` (BARU): scanner + auto-creator
- `guardian_cron.sh` (BARU): cron wrapper
- `main.go` (DIUBAH): +176 baris (createGitHubIssue + handleContribute fallback)
- `reformat_and_comment_issues.py` (referensi): format body issue yang sudah ada
- `document_issues.py` (referensi): logic untuk find/create issue
- `asatidz_master.json`: 283 entries, 115 sudah ada issue, 167 missing
- `kajianlive_mapping.json`: 85 mappings (match_score >= 1.0) untuk enrich body
- `detail/`: 144 files (semua profil yang sudah punya data lengkap)
- `osint_archive/guardian-{date}.log`: log harian guardian
